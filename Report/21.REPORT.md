# UnitConverter_02 — Track A RED U-OUT-02

| 항목 | 내용 |
|------|------|
| 프로젝트 | UnitConverter_02 |
| 단계 | Phase 3 / Cycle 4 — ARRR Ask (RED skeleton) |
| 브랜치 | `red` |
| 보고서 생성일 | 2026-06-11 |
| 목적 | U-OUT-02 `meter:2.5` → `format=json` JSON 문자열 스켈레톤 |

---

## 1. 요약

| 구분 | 결과 |
|------|------|
| Phase | **RED (Ask)** — Cycle 4 Track A P1 |
| Test ID | **U-OUT-02** |
| Trace | **EXT-03** |
| Track | A — Boundary (`tests/boundary/`) |
| Layer | `boundary` — `format_output(..., format="json")` (GREEN) |
| 파일 | `tests/boundary/test_u_out_02.py` (신규) |
| `src/` | **미수정** |
| pytest | **1 failed** (의도적 RED; U-OUT-02 단독) |
| **판정** | **RED 완료** — `/green-minimal` (`green` 브랜치) 대기 |

---

## 2. RED 설계표 (`/red-test-plan`)

### Meta

| Field | Value |
|-------|-------|
| Phase | `red` |
| Layer | `boundary` |
| Track | A — UI/Boundary |
| Test ID | U-OUT-02 |
| Trace | EXT-03 |
| Function | `format_output(converted, format="json")` → JSON string |

### Given / Then

| Given | Then (GREEN) |
|-------|----------------|
| `raw_input="meter:2.5"`, `format="json"` | `json.loads(output)` succeeds; keys include `meter`, `feet`, `yard` |

RED: `pytest.fail("RED: U-OUT-02 — ...")`.

### Path

`tests/boundary/test_u_out_02.py`

### Rules

- `src/` 수정 금지 · domain mock 허용 (Track A) · skip/xfail 금지

---

## 3. 불변식

| Rule | Detail |
|------|--------|
| ECB | `entity` must not import `boundary` |
| Track A | Domain mocks allowed in `tests/boundary/` |

---

## 4. pytest

```text
tests/boundary/test_u_out_02.py::test_u_out_02_meter_2_5_format_json FAILED
```

의도적 RED — GREEN에서 `format_output` JSON 분기 구현.

---

## 5. 다음 단계

- `/green-minimal` on `green` — `format_output(..., format="json")`
- PR #4: 10 intentional failures (entity 5 + boundary 5)

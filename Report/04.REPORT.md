# UnitConverter_02 — Cursor 실습 가이드 (4그룹)

| 항목 | 내용 |
|------|------|
| 프로젝트 | UnitConverter_02 |
| 브랜치 | `spec` |
| 단계 | Spec — Cursor ARRR harness |
| 보고서 생성일 | 2026-06-11 |
| 목적 | 8계층 교육 자료를 **실무 4그룹**으로 단순화 |

---

## 1. 한 장 요약

```
┌─────────────────────────────────────────────────────────┐
│  UnitConverter_02 Cursor 작업                           │
├─────────────────────────────────────────────────────────┤
│  ① 준비물    Harness(pytest) + Rule(.cursor/rules)      │
│  ② 작업 방식 Ask vs Agent + Command(/)                 │
│  ③ 검증      pytest + 추적 ID                          │
│  ④ 문서화    Report/Prompting + /export                │
└─────────────────────────────────────────────────────────┘
```

| 8계층 (교육) | 4그룹 | Cursor |
|--------------|-------|--------|
| Harness | ① 준비물 | `pyproject.toml`, `tests/`, `src/` |
| Rule | ① 준비물 | `.cursor/rules/*.mdc` (루트 `.cursorrules` 없음) |
| Command | ② 작업 방식 | `.cursor/commands/` |
| Agent/Ask | ② 작업 방식 | 채팅 모드 |
| Test Loop | ③ 검증 | `pytest`, trace ID |
| Skill | ④ 문서화 | `unit-converter-tdd`, `unit-converter-docs` |

---

## 2. 주제 (R-G-I-O 확정)

> **한 `unit:value`로 meter·feet·yard를 meter SSOT로 일관 변환하고, 잘못된 입력은 명확히 거부한다.**

성공 기준: [03.REPORT.md](./03.REPORT.md) §3.

---

## 3. Ask vs Agent

| 모드 | 용도 | 예 |
|------|------|-----|
| Ask | 리뷰만, 파일 수정 없음 | "PRD 범위 밖 항목 표만" |
| Agent | 파일·터미널 | `/red-skeleton` |

---

## 4. 슬래시 Command 목록

| Command | ARRR | 모드 |
|---------|------|------|
| `/red-test-plan` | RED 설계표 | Ask |
| `/red-skeleton` | RED 스켈레톤 | Agent |
| `/green-minimal` | GREEN | Agent |
| `/golden-master` | Golden | Agent |
| `/refactor-smell` | REFACTOR 스멜 | Ask |
| `/refactor-safe` | REFACTOR 실행 | Agent |
| `/export` · `/export-session` | Repeat | Agent |

**사용:** `/` + 이름 + Enter (**추가 입력 불필요**).

---

## 5. 파일 치트시트

```text
UnitConverter_02/
├── .cursor/commands/     # ARRR slash commands
├── .cursor/skills/       # unit-converter-tdd, unit-converter-docs
├── docs/PRD.md
├── src/entity/, src/boundary/
├── tests/entity/, tests/boundary/, tests/golden/
├── Report/, Prompting/
└── pyproject.toml
```

---

## 6. 브랜치 (ARRR)

```text
main → spec → red → green → refactor → (repeat) → new_features
```

| 브랜치 | 수정 범위 |
|--------|-----------|
| `spec` | docs, `.cursor/`, Report |
| `red` | `tests/`만 |
| `green` | `src/` + tests |
| `refactor` | 구조만 |

---

## 7. 다음 단계

1. `spec` PR → `main` (사용자 요청 시)
2. `git checkout -b red` from `main`
3. `/red-test-plan` → `/red-skeleton` — **D-CNV-01** Track B

---

## 관련 문서

| 문서 | 설명 |
|------|------|
| [Prompting/04.Export-Transcript.md](../Prompting/04.Export-Transcript.md) | 본 세션 Transcript |
| [WORK_PLAN.md](../WORK_PLAN.md) | Phase 로드맵 |

---

*본 문서는 `Report/04.REPORT.md` — Cursor 4그룹 실습 가이드입니다.*

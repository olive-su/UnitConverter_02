# UnitConverter_02 — Track B RED D-CFG-01

| 항목 | 내용 |
|------|------|
| 프로젝트 | UnitConverter_02 |
| 단계 | Phase 3 / Cycle 3 — ARRR Ask (RED skeleton) |
| 브랜치 | `red` |
| 보고서 생성일 | 2026-06-11 |
| 목적 | D-CFG-01 깨진 JSON → `ConfigError` 스켈레톤 |

---

## 1. 요약

| 구분 | 결과 |
|------|------|
| Phase | **RED (Ask)** — Cycle 3 Track B P1 |
| Test ID | **D-CFG-01** |
| Trace | **EXT-01** |
| Track | B — Logic |
| Layer | `infrastructure` — `config_loader.load_units_json` (GREEN) |
| 파일 | `tests/entity/test_d_cfg_01.py` (신규) |
| `src/` | **미수정** |
| pytest | **1 failed** (의도적 RED; D-CFG-01 단독) |
| **판정** | **RED 완료** — `/green-minimal` (`green` 브랜치) 대기 |

---

## 2. RED 설계표 (`/red-test-plan`)

### Meta

| Field | Value |
|-------|-------|
| Phase | `red` |
| Layer | `entity` / `infrastructure` (config load) |
| Track | B — Logic |
| Test ID | D-CFG-01 |
| Trace | EXT-01 |
| Function | `load_units_json(path)` |

### Given / Then

| Given | Then (GREEN) |
|-------|----------------|
| `tmp_path/broken_units.json` with `{ not valid json` | `pytest.raises(ConfigError)` on `load_units_json(path)` |

RED: `pytest.fail("RED: D-CFG-01 — ...")` — no `config_loader` module yet.

### Path

`tests/entity/test_d_cfg_01.py`

### Rules

- `src/` 수정 금지 · domain mock 금지 · skip/xfail 금지

---

## 3. 불변식

| Rule | Detail |
|------|--------|
| ECB | `entity` must not import `boundary` / `control` |
| Track B | No domain mocks in `tests/entity/` |

---

## 4. pytest

```text
tests/entity/test_d_cfg_01.py::test_d_cfg_01_broken_json_raises_config_error FAILED
```

의도적 RED — GREEN에서 `src/infrastructure/config_loader.py` + `ConfigError` 구현.

---

## 5. 다음 단계

- `/green-minimal` on `green` — `load_units_json` + `ConfigError`
- PR #4: 9 intentional failures (entity 5 + boundary 4)

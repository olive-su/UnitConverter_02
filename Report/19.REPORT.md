# UnitConverter_02 — Track B RED D-REG-01

| 항목 | 내용 |
|------|------|
| 프로젝트 | UnitConverter_02 |
| 단계 | Phase 3 / Cycle 3 — ARRR Ask (RED skeleton) |
| 브랜치 | `red` |
| 보고서 생성일 | 2026-06-11 |
| 목적 | D-REG-01 `register` — cubit 0.4572 등록 후 변환 가능 스켈레톤 |

---

## 1. 요약

| 구분 | 결과 |
|------|------|
| Phase | **RED (Ask)** — Cycle 3 Track B P1 |
| Test ID | **D-REG-01** |
| Trace | **EXT-02**, **NFR-01** |
| Track | B — Logic |
| Layer | `entity` — `unit_registry.register` + `converter.to_meter` |
| 파일 | `tests/entity/test_d_reg_01.py` (신규) |
| `src/` | **미수정** |
| pytest | **1 failed** (의도적 RED; D-REG-01 단독) |
| **판정** | **RED 완료** — `/green-minimal` (`green` 브랜치) 대기 |

---

## 2. RED 설계표 (`/red-test-plan`)

### Meta

| Field | Value |
|-------|-------|
| Phase | `red` |
| Layer | `entity` |
| Track | B — Logic |
| Test ID | D-REG-01 |
| Trace | EXT-02, NFR-01 |
| Function | `register(name, meters_per_unit)` then `to_meter(name, value)` |

### Given / Then

| Given | Then (GREEN) |
|-------|----------------|
| `register("cubit", 0.4572)` then `to_meter("cubit", 1.0)` | result ≈ `0.4572` m (+/- `1e-4`) |

RED: `pytest.fail("RED: D-REG-01 — ...")` — no `unit_registry` module yet.

### Path

`tests/entity/test_d_reg_01.py`

### Rules

- `src/` 수정 금지 · domain mock 금지 · skip/xfail 금지

---

## 3. 불변식

| Rule | Detail |
|------|--------|
| ECB | `entity` must not import `boundary` / `control` |
| Track B | No domain mocks in `tests/entity/` |

---

## 4. pytest

```text
tests/entity/test_d_reg_01.py::test_d_reg_01_register_cubit_then_convertible FAILED
```

의도적 RED — GREEN에서 `src/entity/unit_registry.py` + `to_meter` 연동 구현.

---

## 5. 다음 단계

- `/green-minimal` on `green` — `register` + cubit lookup in `to_meter`
- PR #4: 8 intentional failures (entity 4 + boundary 4)

# UnitConverter_02 — REFACTOR Smell Scan (entity/converter)

| 항목 | 내용 |
|------|------|
| 프로젝트 | UnitConverter_02 |
| 단계 | Phase 5 — ARRR Refine Ask (`/refactor-smell`) |
| 브랜치 | `refactor` (분석 기준: `green` @ `14b860a`) |
| 보고서 생성일 | 2026-06-11 |
| 범위 | `src/entity/converter.py` only — **코드 변경 없음** |

---

## 1. 요약

| 구분 | 결과 |
|------|------|
| Phase | **REFACTOR Ask** — smell table only |
| Tests | `tests/entity/` **6 passed** (green baseline) |
| P0 후보 | 단일 ratio map으로 `to_meter` + `convert_all` 통합 |
| **판정** | `/refactor-safe` 진입 가능 |

---

## 2. Smell table (P0–P2)

| Priority | Smell | Location | Safe refactor candidate | Legacy map (guide/03) |
|----------|-------|----------|-------------------------|------------------------|
| **P0** | Unit 등록이 `to_meter` if-chain과 `convert_all` dict에 **이중 분산** — 단위 추가 시 두 곳 수정 | `converter.py` L9–13, L19–23 | `UNIT_TO_METER` (또는 `constants.py`) 단일 map; `to_meter`/`convert_all`이 map에서 derive | if/elif per unit → OCP (NFR-01) |
| **P0** | 비율 `3.28084`/`1.09361`이 `converter.py`에 하드코딩, `units.json`·테스트와 **SSOT 미연결** | `converter.py` L3–4 | `src/entity/constants.py`로 추출; (P1) `units.json` 로드는 EXT-01 | Hardcoded ratio → EXT-01 |
| **P1** | `to_meter`에 `yard` 입력 경로 없음 — `convert_all`만 yard 출력 | `converter.py` L7–13 | yard→meter factor를 map에 추가 (계약 확장은 RED 후); 현 refactor는 map 구조만 | Partial if/elif coverage |
| **P1** | 단위명 `"meter"`/`"feet"` **매직 스트링** | `converter.py` L9–12, L19–22 | map key로 일원화 | — |
| **P2** | guide/04 권장 `constants.py` 미도입 | `src/entity/` | ratio 상수 파일 분리 | Magic numbers scattered |
| **P2** | `ValueError` 메시지만 — 커스텀 domain error 타입 없음 | `converter.py` L13 | `UnknownUnitError` (boundary 연동 시) | — |

---

## 3. `/refactor-safe` P0 pick (one)

**Candidate:** Extract `METER_TO_FEET` / `METER_TO_YARD` and unit keys into a single `UNIT_TO_METER` map in `src/entity/constants.py`; implement `to_meter` and `convert_all` via that map only.

**Contract preserved:**

- Public signatures unchanged
- Same float outputs → golden `tests/golden/cycle1_track_b/*.json` must still match
- `pytest tests/entity/` 6 passed

**Out of scope for this refactor:**

- `UnitRegistry` class (Cycle 3 D-REG-01)
- `units.json` loader (EXT-01 / D-CFG-01)
- Parser / Formatter split (NFR-02, Cycle 2 Track A)

---

## 4. 다음 단계

1. `git checkout -b refactor` from `green`
2. `/refactor-safe` — P0 map extraction only
3. `pytest tests/entity/ -v` + golden match

---

## 관련 문서

| 문서 | 설명 |
|------|------|
| [Prompting/13.Export-Transcript.md](../Prompting/13.Export-Transcript.md) | Transcript |
| [guide/03_legacy-seed-analysis.md](../guide/03_legacy-seed-analysis.md) | Legacy smell mapping |
| [Report/12.REPORT.md](./12.REPORT.md) | Golden Master |

---

*본 문서는 `Report/13.REPORT.md` — refactor-smell 세션 요약입니다.*
